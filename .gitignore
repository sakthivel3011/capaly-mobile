# Expo / React Native
node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native build output
/android
/ios
*.apk
*.aab
*.ipa

# Environment files — never commit real values
.env
.env.local
.env.*.local

# EAS local credentials
credentials.json

# Firebase config — keep out of git (EAS still bundles it via app.json
# googleServicesFile when building).
google-services.json
GoogleService-Info.plist

# Logs & misc
npm-debug.*
*.log
.DS_Store
*.orig.*
